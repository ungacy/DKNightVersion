//
//  <%= klass.name %>+Night.h
//  <%= klass.name %>+Night
//
//  Copyright (c) 2015 Draveness. All rights reserved.
//
//  These files are generated by ruby script, if you want to modify code
//  in this file, you are supposed to update the ruby code, run it and
//  test it. And finally open a pull request.

#import <UIKit/UIKit.h>
#import "NSObject+Night.h"

@interface <%= klass.name %> (Night)

<% klass.properties.each do |property| %><%= "@property (nonatomic, copy, setter = dk_set#{property.cap_name}Picker:) DKColorPicker dk_#{property.name}Picker;\n"%><% end %>
@end

@interface <%= klass.name %> (IBInspectable)

<% klass.properties.each do |property| %><%= "@property (nonatomic, strong) IBInspectable UIColor *normal#{property.cap_name};\n"%><% end %>
<% klass.properties.each do |property| %><%= "@property (nonatomic, strong) IBInspectable UIColor *night#{property.cap_name};\n"%><% end %>

@end
